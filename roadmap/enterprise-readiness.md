
# Enterprise readiness (MVP checklist)

Must-have:
- SSO (SAML/OIDC) + RBAC
- Audit logs (admin + policy decisions + run actions) + export
- Tenant isolation story (cell-based) + per-tenant keys
- Data residency statement (US+EU pinning)
- Encryption in transit/at rest, secrets mgmt
- Incident response process + on-call
- Security questionnaire pack starter (architecture 1-pager + data flows)

v1 commitments:
- SCIM
- SOC2 Type I→II path
- BYOK, private connectivity enhancements
